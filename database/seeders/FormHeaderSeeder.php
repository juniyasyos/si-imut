<?php

// Deprecated: FormHeaderSeeder replaced by EnhancedFormBuilder/FormTemplate seeders.
// The original implementation is intentionally removed. Keep this file as a stub to avoid
// accidental autoload/classmap errors in environments that still reference it.

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class FormHeaderSeeder extends Seeder
{
    public function run(): void
    {
        // No-op: legacy seeder removed. Use EnhancedFormBuilderSeeder or HandHygieneFormSeeder instead.
        $this->command->info('FormHeaderSeeder is deprecated and has been disabled.');
    }
}
